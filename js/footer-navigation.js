/**
 * Footer Navigation Script
 * Общий модуль для всех страниц проекта Manasik Travel
 * Отвечает за обработку кликов по ссылкам в футере
 * и навигацию между страницами (роутинг).
 *
 * Версия: 1.0
 * Автор: Manasik Travel Dev Team
 */

document.addEventListener("DOMContentLoaded", () => {
  const footer = document.querySelector(".footer");
  if (!footer) return;

  // Находим все ссылки в футере
  const links = footer.querySelectorAll("a[href]");

  // Список маршрутов (ты добавишь свои значения)
  const routes = {
    "/": "/", // Главная
    "Туры в Хадж": "tours-hadj.html", 
    "Необходимые документы на Хадж": "#",
    "Информация о дате начала хаджа": "#",
    "Проверить доплату за повторный Хадж": "#",
    "новости": "#",
    "Туры на Умру": "tours-hadj.html",
    "Необходимые документы на Умру": "#",
    "Проверить доплату за повторную Умру": "#",
    "Советы паломникам": "#",
    "Уроки": "#",
    "Часто задаваемые вопросы": "#",
    "Адреса вакцинаций": "#",
    "Фото хаджа и умры": "#",
    "Видео хаджа и умры": "#",
    "Онлайн трансляция Мекки": "#",
    "Онлайн трансляция Медины": "#",
    "О нас": "#",
    "Филиалы и контакты": "#",
    "Сотрудничество": "#",
    "Обратная связь": "#",
    "Банковские реквизиты": "#",
    "Вакансии": "#",
    "Пресса о нас": "#",
    "Проверить договор": "#",
    "Узнать дату вылета": "#",
    "Проверка готовности визы": "#",
    "Отзывы": "#",
  };

  // Обработчик кликов по ссылкам футера
  links.forEach((link) => {
    link.addEventListener("click", (event) => {
      const text = link.textContent.trim();

      // Прерываем стандартное поведение
      event.preventDefault();

      // Проверяем, есть ли этот пункт в маршрутах
      if (routes[text]) {
        console.log(`Переход к: ${text} (${routes[text]})`);
        // 👇 Здесь ты добавишь свой роутер
        // Например:
        window.location.href = routes[text];
        // или
        // navigateTo(routes[text]);
      } else {
        console.warn(`Маршрут для "${text}" не найден.`);
      }
    });
  });

  console.log("Footer navigation initialized ✅");
});
