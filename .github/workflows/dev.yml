name: Build on Develop Push

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Клонировать репозиторий
        uses: actions/checkout@v3

      - name: 🛠 Архивировать проект
        run: |
          tar -czf build-dev.tar.gz *
